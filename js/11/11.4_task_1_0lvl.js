//Что вы думаете? Выполнится ли .catch? Поясните свой ответ.

new Promise(function(resolve, reject) {
  setTimeout(() => {
    throw new Error("Whoops!");
  }, 1000);
}).catch(alert);

// не выполнится, при использовании промисов, ошибки, которые генерируются асинхронно, 
// должны быть явно обработаны внутри промиса с помощью метода reject или переданы дальше
// через цепочку промисов с использованием catch().